name: CI
on: 
  push:
    branches:
      - main
jobs:
  deploy:
    name: deployruns-on
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: goto-bus-stop/setup-zig@v2
        with:
          version: 0.15.2
      - name: build
        run: zig build
    
  
